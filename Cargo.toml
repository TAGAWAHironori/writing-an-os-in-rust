[package]
name = "blog_os"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bootloader = "0.9.8"
# panicで中止するための設定
[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
